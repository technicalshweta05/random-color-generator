# random-color-generator
This project generates random RGB background colors on button click using JavaScript's `Math.random()`. Each click produces random red, green, and blue values (0-255), dynamically updating the page background. It demonstrates DOM manipulation, event handling, and random color generation concepts.
